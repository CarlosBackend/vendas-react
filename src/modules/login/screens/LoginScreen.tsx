import { BackGroundImage } from '../../styles/loginScreen.style';

const LoginScreen = () => {
  return (
    <div>
      <BackGroundImage src="./logo.png" />
    </div>
  );
};

export default LoginScreen;
